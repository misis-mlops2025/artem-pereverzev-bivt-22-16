data:
  seed: 42
  n_samples: 1000
  noise_level: 0.1

prepare:
  seed: 42
  test_size: 0.2

preprocess:
  scaler_type: "robust"
  handle_unknown: "ignore"

train:
  model_type: "random_forest"
  random_state: 42
  n_estimators: 100
  max_depth: 10

predict:
  batch_size: 32